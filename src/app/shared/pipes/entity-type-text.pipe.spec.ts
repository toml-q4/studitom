import { EntityTypeTextPipe } from './entity-type-text.pipe';

describe('EntityTypeTextPipe', () => {
  it('create an instance', () => {
    const pipe = new EntityTypeTextPipe();
    expect(pipe).toBeTruthy();
  });
});
