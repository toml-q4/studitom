import { JiraDirective } from './jira.directive';

describe('JiraDirective', () => {
  it('should create an instance', () => {
    const directive = new JiraDirective();
    expect(directive).toBeTruthy();
  });
});
