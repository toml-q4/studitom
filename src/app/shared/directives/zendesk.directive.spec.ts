import { ZendeskDirective } from './zendesk.directive';

describe('ZendeskDirective', () => {
  it('should create an instance', () => {
    const directive = new ZendeskDirective();
    expect(directive).toBeTruthy();
  });
});
